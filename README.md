# microEye
A python toolkit for fluorescence microscopy that features IDS uEye industrial-grade CMOS cameras.

This is a version that works without qt - mostly reconstruction algorithms only. To install this package:

pip install -r requirements.txt 
pip install . --no-deps 